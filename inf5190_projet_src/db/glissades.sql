CREATE TABLE glissades(
    id integer primary key,
    nom varchar(256),
    nom_arrondissement varchar(256),
    cle_arrondissement varchar(256),
    date_maj_arrondissement varchar(256),
    ouvert number(1),
    deblaye number(1),
    condition varchar(256)
);