CREATE TABLE patinoires(
    id integer primary key,
    nom varchar(256),
    nom_arrondissement varchar(256),
    date_maj varchar(256),
    ouvert number(1),
    deblaye number(1),
    arrose number(1),
    resurface number(1)
);